<menu xmlns:android="http://schemas.android.com/apk/res/android"
    	xmlns:wawafinder="http://schemas.android.com/apk/res-auto">
        
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_near_me"
          android:title="@string/action_near_me"
          android:icon="@drawable/ic_action_near_me" 
          wawafinder:showAsAction="ifRoom" />
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_search"
          android:title="@string/action_search"
          android:icon="@drawable/ic_action_search" 
          wawafinder:showAsAction="never"/>
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:icon="@drawable/ic_action_settings" 
          wawafinder:showAsAction="never"/>
   	<item android:id="@+id/action_exit"
          android:title="@string/action_exit"
          android:icon="@drawable/ic_action_exit" 
          wawafinder:showAsAction="never"/>
        
</menu>
